# TA-P4-009 E2E 主链路（2026-03-03）

- Task ID：TA-P4-009
- 阶段：Phase 4
- 状态：DONE
- 负责人角色：QA Engineer / Backend Engineer

## 1. 目标

覆盖消息通道主链路 E2E：A 建群 -> 邀请 B -> B 接受 -> 群聊（文本/图片/文件）成功，确保 API、成员状态与消息投递行为闭环。

## 2. 实现

- 新增 E2E 测试文件：`packages/node/src/phase4-e2e.test.ts`
  - 引入可控时钟、内存态 `FakeGroupService` 与 API Server 测试上下文。
  - 通过真实 `/api/v1/groups*` 路径执行建群、邀请、接受流程。
  - 在消息链路中覆盖三类内容：
    - 文本消息（`contentType=text`）
    - 图片消息（`attachments/init-upload` + `complete-upload` + `contentType=image`）
    - 文件消息（`attachments/init-upload` + `complete-upload` + `contentType=file`）
  - 校验 `GET /api/v1/messages/pull` 返回会话内有序 `seq=[1,2,3]`，且 `provisional=false`。

## 3. 验证结果

- 新增测试用例：
  - `TA-P4-009 E2E main path: create -> invite -> accept -> group chat (text/image/file)`
- Node 包测试：`docs/implementation/phase-4/logs/2026-03-03-p4-node-test.txt`
- 工作区回归：`docs/implementation/phase-4/logs/2026-03-03-p4-workspace-test.txt`
- 结论：主链路 E2E 闭环通过，文本/图片/文件链路已纳入自动化回归。

## 4. 下一步

推进 `TA-P4-011`（<=500 成员压测与 SLO 验证）。
